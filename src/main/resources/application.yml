server:
  port: 8610

spring:
  application:
    name: i_learn
  profiles:
    active: dev
#mybatis:
#  mapper-locations: classpath:mybatis/mapper/**/*.xml
#  type-aliases-package: com.bailu.**.domain,;com.bailu.**.vo
#  configuration:
#    # 开启驼峰转换
#    map-underscore-to-camel-case: true
#    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

# JWT 配置
jwt:
  # 认证名称
  header: i_learn
  # 加密字符串
  secret: eCdkjc6ESc4mbnU8
  # token 过期时间: 168 小时 (7天)
  expiration: 168


mybatis-plus:
  mapper-locations: classpath:mybatis/mapper/**/*.xml
  type-aliases-package: com.bailu.**.domain,;com.bailu.**.vo
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-value: 1
      logic-not-delete-value: 0
  type-enums-package: com.bailu.sterilizetracing.*
